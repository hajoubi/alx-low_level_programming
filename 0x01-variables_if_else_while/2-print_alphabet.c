#include <stdio.h>
/**
 *main - Entry point
 *Description: 'a program that prints alphabets lower case using putchar'
 *Return: value 0 no problem
*/
int main(void)
{
   for (char c = 'a'; c <= 'z'; c++)
   {
        putchar(c);
   }
    return (0);
}
