#include <stdio.h>

/**
 * main - prints the phrase.
 * 
 * Return: 0 if exited with no problems.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
